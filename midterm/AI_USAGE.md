# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:**  นาย กิตติทัต กันธรรม **รหัส:** 68543210019-4 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 11:15 | B1-1 | "สาเหตุเกิดขึ้นที่ไฟล์ไหนเกิดขึ้นเพราะ ไม่มี response.ok ใช่ไหม" | ใช่ครับ สาเหตุหลักเกิดจากการที่ไม่ได้เช็ก response.ok ตามที่คุณคาดการณ์ไว้ | ไปไล่ดูไฟล์ week 5 และเจอปัญหาว่าตัวต้นเรื่องอยู่ที่ key ไม่ได้กำหนดที่ไฟล์ RequestList บรรทัด 8 และได้เช็กกับ AI อีกครั้งจนแก้บั๊กสำเร็จ |
| 12:10 | B1-2 | "ตัวดำเนินการไม่อัปเดตตาม state... แต่ไม่มี key id เลยไม่สามารถอัปเดตข้อมูลได้ เข้าใจถูกไหม" | ต้นตอของปัญหาไม่ได้อยู่ที่ key id แต่อยู่ที่ไฟล์ DashboardPage.jsx บรรทัด 47 ที่ทำให้ตัวกรองไม่อัปเดตตามคลิก | เข้าไปตรวจสอบและแก้ไขโค้ดตรรกะตัวกรองที่ไฟล์ DashboardPage.jsx |
| 12:40 | B1-4 | "ปัญหาคือกดแล้วหน้าไม่เปลี่ยนอัตโนมัติ ต้องให้ผู้ใช้กด F5 หรือรีเฟรชเองถึงจะเห็นหน้านั้นๆ (เช่น REQ-002)" | ปัญหาอยู่ที่ `useEffect` ใน `RequestDetailPage.jsx` ไม่ได้ใส่ `requestId` ไว้ใน Dependency Array ทำให้ React ไม่รู้ว่าต้องดึงข้อมูลใหม่ | เข้าไปแก้ไฟล์ RequestDetailPage.jsx บรรทัด 28 โดยเติม `requestId` เข้าไปในวงเล็บ `[requestId, reloadKey]` |
| 13:18 | B2.1 & B2.2 | "CP-B2.1 && CP-B2.2 มีขั้นตอนการทำอย่างไรบ้าง และทำไม `filteredRequests` ถึงขึ้น error แจ้งว่าใส่ซ้ำไม่ได้" | แนะนำให้รวมตรรกะการค้นหา (searchQuery) และตัวกรองสถานะ (statusFilter) เข้าด้วยกัน และให้ลบประกาศตัวแปร `filteredRequests` ตัวเก่าทิ้งเพื่อป้องกัน Error | ลบโค้ด `filteredRequests` ชุดเก่าทิ้ง และแทนที่ด้วยบล็อกโค้ดใหม่ที่เช็กทั้ง `matchStatus` และ `matchSearch` (`toLowerCase().includes()`) พร้อมนำ `<input>` ไปวางแทรกใน `DashboardPage.jsx` |
| 13:45 | B2.4 | "วิธีเพิ่มรายการทั้งหมด รอดำเนินการ กำลังดำเนินการ เสร็จสิ้นต้องเพิ่มอะไรใน RequestList บ้าง" | ใน `DashboardPage` ให้แผงสรุปนับจาก `requests` (ข้อมูลทั้งหมด) ห้ามนับจาก `filteredRequests` และใน `RequestList` ให้เขียนเงื่อนไข `requests.length === 0` เพื่อแสดงข้อความไม่พบคำร้อง | ปรับ `SummaryPanel` ให้นับค่าจากข้อมูลตั้งต้นเสมอ และเพิ่มเงื่อนไขแสดง `<p className="subtle-empty">ไม่พบคำร้องที่ตรงกับการค้นหา</p>` ในคอมโพเนนต์ `RequestList` |
| 14:00 | B3.1 & B3.2 | "เพิ่มปุ่มทำเสร็จ ต้องทำอะไรยังไงบ้าง (กดแล้วปุ่มไม่ขึ้น)" | แนะนำให้เพิ่ม `onMarkDone` เข้าไปรับเป็น prop ใน `RequestCard.jsx` และตรวจเช็กการส่งทอด prop จาก `DashboardPage` ลงมา | เข้าไปแก้ไขรับค่า `onMarkDone` ใน `RequestCard` และเรียกใช้ฟังก์ชัน `updateRequestStatus` ในหน้า Dashboard แล้วสั่ง `setRequests(nextRequests)` ให้โหลดข้อมูลใหม่ |
| 14:30 | B4.1 - B4.3 | "CP-B4.2 จัดการ edge case ถ้า priority เป็นค่าอื่นต้องแสดง 'ไม่ระบุ' แต่หน้าจอแสดงข้อความติดกัน" | สาเหตุเกิดจากลืมลบโค้ดทดสอบชั่วคราว (`<PriorityBadge priority="high"/>`) ออกจากไฟล์ `RequestCard.jsx` | เข้าไปเขียนเงื่อนไข 3 กรณีดักค่าว่างใน `PriorityBadge.jsx` จากนั้นเข้าไปลบโค้ดบรรทัดทดสอบชั่วคราวออกจากการ์ดคำร้อง เพื่อให้แสดงผลป้ายสถานะถูกต้องเพียงป้ายเดียว |
| 14:45 | B4.3 (Review) | ส่งโค้ด `DashboardPage.jsx` ฉบับเต็มให้ช่วยตรวจสอบความเรียบร้อย | แนะนำให้ลบ `<RequestList>` ที่เรียกซ้ำซ้อนกัน 2 อันทิ้ง และแนะนำการลบปีกกา `{}` ที่ครอบแท็ก JSX ออก | เข้าไปลบ `<RequestList>` บรรทัดล่างที่เกินมาทิ้ง และจัดระเบียบโค้ดจนหน้าเว็บแสดงผลสมบูรณ์ ไม่มีการ์ดซ้ำซ้อน |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** นาย กิตติทัต กันธรรม
